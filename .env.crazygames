# Production build for CrazyGames. Loaded only via `--mode crazygames`
# (wired into `npm run build`). Overrides .env to strip every dev affordance:
# no window.dev console namespace, no in-panel cheat buttons.
VITE_DEV_MODE=false
